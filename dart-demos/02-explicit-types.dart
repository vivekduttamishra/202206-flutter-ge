main(){
  int x = 30;
  int y = 20;
  // dividing int with int = double
  int sum = x+y;


  // any datatype will need to be converted before contactinating with string
  print("Sum of "+x.toString()+" with "+ y.toString()+ " is "+ sum.toString());
  print("Dividing $x by $y returns ${x/y}");
}